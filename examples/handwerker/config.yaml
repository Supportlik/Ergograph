# Persona example: master carpenter (fictional) — shows a trade CV with
# certificates (Meisterbrief), project groups and an empty publications list.
person:
  name: Thomas Brandl

theme: modern
level_max: 6

languages: [de]
documents: [full]

content:
  de: content/de.yaml

output:
  html_dir: .build/html
  pdf_dir: pdf
  date_prefix: false
